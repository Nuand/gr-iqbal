# auto-generated by grc.converter

id: iqbalance_fix_cc
label: IQ Bal Fix
category: IQ Balance

parameters:
-   id: mag
    label: Magnitude
    dtype: real
-   id: phase
    label: Phase
    dtype: real

inputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: iqbal_corr
    optional: true

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import iqbalance
    make: iqbalance.fix_cc(${mag}, ${phase})
    callbacks:
    - set_mag(${mag})
    - set_phase(${phase})

file_format: 1
